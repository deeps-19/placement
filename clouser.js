//write a code on closures and explain
function print()
{
    function second()
    {
        console.log("inside function");
    }
     second();
   //if function call is outside the fuction then its get error that function is not define
}

print()
